{% extends 'base.html.twig' %}

{% block title %}Famille index{% endblock %}

{% block body %}
    <h1>Famille index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for famille in familles %}
            <tr>
                <td>{{ famille.id }}</td>
                <td>{{ famille.nom }}</td>
                <td>
                    <a href="{{ path('app_famille_show', {'id': famille.id}) }}">show</a>
                    <a href="{{ path('app_famille_edit', {'id': famille.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_famille_new') }}">Create new</a>
{% endblock %}
